import React from 'react';

const PhotosPage: React.FC = () => {
  const onDateSelect = (date: Date) => {
    // TODO: implement date selection handling logic.
  };

  return <div>Photos Page</div>;
};

export default PhotosPage;
